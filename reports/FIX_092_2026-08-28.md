# Fix 092: actual KYC/support definitions

Prepared from the seven function definitions supplied by the user, not guessed signatures.

## Changes

- KaFarm reply: require login; make all authorization boolean terms NULL-safe; lock the chat row before checking closed state and inserting a reply.
- KYC: require a customer profile; serialize submissions with a per-profile advisory lock; lock an existing KYC row; allow only initial/draft/not-submitted or explicit rejected/needs-info submissions. Other states, including approved and under-review states, cannot overwrite data/documents or reset verification. The original ID normalization, validation, risk calculation and evidence/inbox writes are retained after the guard.
- Customer/caretaker support: lock the owned session and reject ended/completed conversations before inserting messages. The frontend sends new messages to a new conversation if closure is already known; if closure races submission, it reconciles/retire-checks the operation before offering a new send. It does not reopen old history.
- Anonymous/PUBLIC execution revoked on the four replaced RPCs; authenticated access retained with internal checks.

## Verification

31 local tests/checks PASS: includes actual transpiled frontend handlers with mocked dependencies and five SQL-source contracts. These do not execute PostgreSQL or prove deployed RLS/grants/concurrency behavior. Build and targeted lint run separately in this task.

## Deployment

Review/test `database/applied/092_kyc_and_support_state_guards.sql` on an isolated project before production. Run after 091; deploy matching frontend. Migration changes function definitions/grants only, not existing customer/message/KYC records. During future legitimate rejected-KYC resubmission, the existing function still replaces document rows as before.

No SQL was executed remotely by this task and nothing was pushed.

Remaining verification: owner/non-owner/anonymous reply calls, two simultaneous KYC submissions, approval-vs-resubmission race, both roles messaging a closed conversation, and retry after a lost response in a real isolated database. SQL 092 verification output checks installed source markers, not an end-to-end workflow result.
